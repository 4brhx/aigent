const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];function esc(s){return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}function fmt(t){let s=esc(t);s=s.replace(/```([\s\S]*?)```/g,(_,c)=>'<pre><code>'+c+'</code></pre>');s=s.replace(/`([^`]+)`/g,'<code>$1</code>');s=s.replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>');return s;}function timeStr(ts){return new Date(ts).toLocaleTimeString('ar',{hour:'2-digit',minute:'2-digit'})}function renderConvList(filter){filter=filter||'';const el=$('#convList');if(!el)return;const f=filter.toLowerCase();const items=state.conversations.filter(c=>!f||c.title.toLowerCase().includes(f));if(!items.length){el.innerHTML='<div class="conv-empty">لا توجد محادثات</div>';return;}const groups={};items.forEach(c=>{const g=groupLabel(c.ts);(groups[g]=groups[g]||[]).push(c);});const order=['اليوم','أمس','هذا الأسبوع','أقدم'];el.innerHTML=order.filter(g=>groups[g]).map(g=>'<div class="conv-group">'+g+'</div>'+groups[g].map(c=>{const m=getModel(c.modelId);return '<button class="conv-item'+(c.id===state.activeId?' active':'')+'" data-id="'+c.id+'"><span class="c-icon">'+m.icon+'</span><span class="c-title">'+esc(c.title)+'</span><span class="c-actions"><button class="c-btn" data-act="rename" data-cid="'+c.id+'">&#9998;</button><button class="c-btn del" data-act="del" data-cid="'+c.id+'">&#10005;</button></span></button>';}).join('')).join('');}function renderModelGrid(){const el=$('#modelGrid');if(!el)return;el.innerHTML=MODELS.map(m=>'<button class="m-card'+(m.id===state.modelId?' selected':'')+'" data-mid="'+m.id+'"><span class="m-icon">'+m.icon+'</span><span class="m-name">'+esc(m.short)+'</span><span class="m-vendor">'+esc(m.vendor)+'</span><span class="m-check">&#10003;</span></button>').join('');}function renderPill(){const c=activeConv();const m=getModel(c?c.modelId:state.modelId);$('#pillIcon').innerHTML=m.icon;$('#pillName').textContent=m.name;}function renderDropdown(){const el=$('#modelMenu');if(!el)return;const cur=activeConv()?activeConv().modelId:state.modelId;el.innerHTML=MODELS.map(m=>'<button class="dd-row'+(m.id===cur?' active':'')+'" data-mid="'+m.id+'"><span class="m-icon">'+m.icon+'</span><span style="flex:1"><div style="font-size:13px;font-weight:600">'+esc(m.name)+'</div><div class="dd-meta">'+esc(m.vendor)+' · '+esc(m.tag)+'</div></span>'+(m.id===cur?'&#10003;':'')+'</button>').join('');}function renderSuggestions(){const data=[{h:'اشرح مفهوماً',p:'بسّط لي Transformers بأسلوب سهل.'},{h:'اكتب كود',p:'دالة Fibonacci بطريقة Memoization.'},{h:'أفكار مشاريع',p:'5 أفكار SaaS قابلة للتنفيذ.'},{h:'ساعدني بالكتابة',p:'بريد اعتذار عن تأخّر مشروع.'}];const el=$('#suggestions');if(!el)return;el.innerHTML=data.map(function(s,i){return '<button class="sug" data-si="'+i+'"><span class="sug-h">'+s.h+'</span><span class="sug-p">'+s.p+'</span></button>';}).join('');window._sugData=data;}function renderChat(){const c=activeConv();const ol=$('#messages');ol.innerHTML='';if(!c||!c.messages.length){$('#emptyState').style.display='';return;}$('#emptyState').style.display='none';c.messages.forEach(function(m){if(m.role==='user')addUserBubble(m.text,m.ts,false);else addAiBubble(m.text,m.modelId||c.modelId,m.ts,false);});scrollEnd(true);}function addUserBubble(text,ts,anim){ts=ts||Date.now();const li=document.createElement('li');li.className='msg user';if(!anim)li.style.animation='none';li.innerHTML='<div class="msg-row"><div class="bubble">'+fmt(text)+'</div></div><div class="msg-meta">'+timeStr(ts)+' · أنت</div>';$('#messages').appendChild(li);scrollEnd();}function addAiBubble(text,modelId,ts,anim,placeholder){ts=ts||Date.now();const m=getModel(modelId);const li=document.createElement('li');li.className='msg ai';if(!anim)li.style.animation='none';li.innerHTML='<div class="msg-row"><div class="ai-icon">'+m.icon+'</div><div class="bubble">'+(placeholder?'<div class="typing"><i></i><i></i><i></i></div>':fmt(text||''))+'</div></div><div class="msg-meta">'+esc(m.short)+' · '+timeStr(ts)+'<span class="msg-actions"><button class="act-btn" data-act="copy">&#128203;</button><button class="act-btn" data-act="regen">&#8634;</button></span></div>';$('#messages').appendChild(li);scrollEnd();return li;}function scrollEnd(instant){const el=$('#chatArea');if(!el)return;el.scrollTo({top:el.scrollHeight,behavior:instant?'auto':'smooth'});}function toast(msg){const t=$('#toast');if(!t)return;t.querySelector('.toast-msg').textContent=msg;t.classList.add('show');clearTimeout(t._tid);t._tid=setTimeout(function(){t.classList.remove('show');},1800);}function updateTitle(){const c=activeConv();$('#chatTitle').textContent=c?c.title:'محادثة جديدة';}
